# Tercih edilen kurulum `pip install .[gui,web]` + `.[dev]`'tir (pyproject.toml kaynaktır).
# Bu dosya hızlı/CI kurulumu içindir: GUI + web + geliştirme/test bağımlılıkları,
# kurulu sürümlere pinlenmiş. Temel bağımlılıkları da çeker (-r requirements.txt).

-r requirements.txt

# GUI extra (.[gui])
customtkinter==5.2.2

# Web/API extra (.[web])
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.30

# Geliştirme/test extra (.[dev])
pytest==9.0.3
ruff==0.15.15
httpx==0.28.1
